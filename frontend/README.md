# Smart Home Frontend

Frontend React/Vite de l'application Smart Home.

## Scripts

```bash
npm install
npm run dev
npm run build
```

Le proxy Vite redirige `/api` vers le backend Express, par defaut sur `http://localhost:5000`.
